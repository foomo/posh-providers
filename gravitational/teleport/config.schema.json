{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/foomo/posh-providers/main/gravitational/teleport/config.schema.json",
  "$ref": "#/$defs/Config",
  "$defs": {
    "Config": {
      "properties": {
        "path": {
          "type": "string",
          "description": "Directory holding the teleport session state, exported as TELEPORT_HOME\nwhen the provider is constructed. Relative to the project root."
        },
        "labels": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object",
          "description": "Labels every cluster, app and database must carry to be listed. Combined\ninto a single tsh --query expression with \u0026\u0026; omit to list everything."
        },
        "hostname": {
          "type": "string",
          "description": "Teleport proxy to authenticate against, e.g. \"teleport.example.com:443\"."
        },
        "kubernetes": {
          "$ref": "#/$defs/Kubernetes",
          "description": "Kubernetes-specific settings."
        },
        "apps": {
          "additionalProperties": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "type": "object",
          "description": "Extra arguments passed to `tsh apps login`, keyed by app name. Prepended\nbefore the app name itself; apps not listed here still work."
        },
        "database": {
          "$ref": "#/$defs/Database",
          "description": "Database-specific settings."
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "Database": {
      "properties": {
        "user": {
          "type": "string",
          "description": "Database user to log in as. Overridden by the TELEPORT_DATABASE_USER\nenvironment variable when it is set."
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "Kubernetes": {
      "properties": {
        "aliases": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object",
          "description": "Display names for kube clusters, keyed by the real teleport cluster name.\nCompletion offers the alias and it is mapped back before login, so the\nvalues must be unique and must not collide with another cluster's real\nname; a configuration that breaks either rule is rejected when the alias\nis resolved."
        }
      },
      "additionalProperties": false,
      "type": "object"
    }
  }
}